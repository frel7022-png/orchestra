# 아카이브 — 제거된 기능 / 조사하다 보류한 이슈 / 과거 설계 변경 경위

이 문서는 `CLAUDE.md`에서 분리한 "평소엔 안 읽어도 되는데, 가끔 결정적인 단서가 되는" 내용이다
(2026-08-25 분리). `CLAUDE.md`처럼 세션 시작할 때 자동으로 로드되지 않으므로, **문제가 안
풀리거나 이상한 동작을 발견했을 때 일부러 열어봐야 한다.** 섹션 번호는 `CLAUDE.md`의 번호를
그대로 따른다(예: 여기 "§6-9"는 `CLAUDE.md` §6-9의 상세 경위).

아무 내용도 삭제하지 않고 `CLAUDE.md`에서 그대로 옮겨온 것 — git log에 각 변경의 커밋도
남아있으니 이중으로 안전하다.

---

## §1-7. "섹터 일괄 수정" — 문서엔 있었는데 실제로는 없었던 사례

"종목 편집"(수동 종목 추가/평단가/섹터 editor)을 삭제 요청받은 뒤, 대신 **섹터만 고칠 수 있는
가벼운 도구**("섹터 일괄 수정")를 별도로 다시 만들었다고 이 문서에 한동안 적혀있었는데,
**2026-08-24에 확인해보니 실제로는 화면 어디에도 없고 `portfolio_core.update_sector_cache()`
함수만 코드에 남아있을 뿐 어디서도 호출되지 않는 상태였음**(§6-6 v2→v3 때 겪은 것과 같은
유형의 "문서엔 있는데 실제로는 없는" 사례 — 재발 주의, [[feedback_check_uncommitted_local_state_files]]
참고). "미분류" 종목은 지금은 세션이 그때그때 네이버 등에서 찾아서 `update_sector_cache()`를
직접 호출해 캐시에 저장하는 방식으로 처리 중(2026-08-24, 11개 종목 일괄 처리함) — 섹터 캐시가
텅 빈 상태로 시작하면(신규 종목 대량 유입 등) 여전히 이런 도구가 필요하므로, 재구현하려면 이
함수를 UI에 연결하기만 하면 됨.

---

## §4. 거래 기록 탭 — 초기 기획 문서와 실제 구현이 어긋났던 사례 (2026-08-24 발견)

`CLAUDE.md` §4의 "거래 기록 탭" 목록은 초기 기획 당시 적힌 것으로 보이고, 실제 구현과 다른
항목이 섞여있음이 확인됐었다:
- ~~매매일지 CSV 업로드~~ → 실제로는 앱 안에 업로드 위젯이 없음. §6-2대로 `ingest_daily.py`를
  세션이 직접 실행하는 방식.
- ~~섹터 일괄 수정~~ → 위 §1-7 참고, 실제로는 없었음.
- ~~GitHub 동기화 상태~~ → §6-1대로 앱 코드에 GitHub 연동 자체가 없으므로 이 진단 화면도
  존재하지 않았음.
- 거래 내역 캘린더만 실재하는 기능이었음(`ui_transactions_tab.py`에 캘린더 이전/다음 버튼
  코드로 확인됨).

이후 `CLAUDE.md` §4를 실제 구현 기준으로 다시 정리함(2026-08-25).

---

## §6-1-2. Streamlit Cloud 배포 지연 — 재발 사례

2026-08-21 물타기 그래프 기능 최초 배포 때, 2026-08-24 WATERING 칩 UI 변경 때 둘 다 겪음 —
사용자가 재발 사실을 직접 확인해줌. 강제 새로고침(Ctrl+Shift+R)/시크릿창으로도 즉시 해결 안 될
수 있음 — 원인은 특정되지 않았으나(브라우저/기기 쪽 캐싱으로 추정) 코드나 배포 자체의 문제는
아닌 것으로 보임.

---

## §6-4. 앱 "실현손익" vs 증권사 "당일실현" 오차 — 조사만 하고 수정은 보류

`apply_transaction()`(portfolio_core.py)의 `realized = (price - old_avg) * qty`는
수수료/세금을 차감하지 않은 값이라, 증권사가 보여주는 "당일실현"(수수료+세금 차감 후)보다 앱
쪽 실현손익이 항상 조금 더 크게 나온다. **현금(예수금) 잔액 자체는 정확함** — fee가 현금
차감에는 반영되고 있음. 부풀려 보이는 건 "실현손익" 표시값 하나뿐.

원본 증권사 CSV에는 이미 수수료+세금까지 반영된 실현손익 컬럼(`parse_daily_trade_csv`의
`실현손익_증권사`, 위치 `c[12]`)이 있는데, `import_daily_trades()`가 이 값을 버리고 빈
문자열로 저장하고 있다 (해당 함수 내 `"실현손익": ""` 부분). 이 컬럼을 그대로 쓰면 증권사
숫자와 정확히 일치할 가능성이 높다 — 확인은 했으나 수정은 안 함.

사용자가 "몇백 원 차이니 넘어가자"고 명시적으로 판단(2026-08-18). 나중에 정확히 맞추고
싶다는 요청이 오면 이 메모를 참고해서 바로 고칠 것.

---

## §6-6. Fishing — v2→v3 전환 경위, 그리고 제거된 부속 기능들

### v2→v3 변경 이유 (실제 겪은 버그)
v2는 최초가를 `watchlist_prices.csv`에 "영구 보존"하는 방식이었는데, **이 파일이 처음부터
git에 한 번도 커밋된 적이 없었다**(당시 문서엔 "git에 커밋되는 영구 데이터"라고 잘못
적혀있었음 — 실제로는 안 지켜지고 있었던 걸 아무도 몰랐음). Streamlit Cloud는 git push할
때마다 재배포되면서 로컬 파일시스템을 통째로 새로 만드는데(§1-5), 이 파일은 앱이 직접 쓰는
파일이라 세션의 커밋 루틴에도 낄 수가 없어서 **push할 때마다 사라지고, 사라질 때마다 최초가가
그 시점 기준 "어제 종가"로 리셋**되는 버그가 있었다(2026-08-19~21에 DB 작업하며 push가
잦아져서 버그가 훨씬 자주 터져서 발견됨 — 누적%와 전일%가 항상 똑같이 나오는 증상). §6-9의
Supabase `price_history`는 앱 재배포와 무관하게 독립적으로 유지되므로 최초가 소스를 거기로
바꿔서 이 문제를 구조적으로 없앰 — 로컬 캐시 파일(`watchlist_prices.csv`) 자체가 필요 없어져서
완전히 삭제함(`load_watchlist_prices`/`save_watchlist_prices`/`WATCHLIST_PRICES_FILE`도
코드에서 제거).

### (제거됨) "오늘의 순위" TOP20
한 번 만들었다가(전일 대비 하락 TOP20, 접었다 펴는 버튼) v2로 개편하면서 사용자 요청으로
제거함 — "기준"(누적/전일) + "방향"(DOWN/UP) 라디오 조합으로 대체됐음. 재도입하지 말 것.

### (제거됨) Fishing 안의 "패턴 검색" — 하락 후 횡보 스크리너 (2026-08-18 신설 → 2026-08-19 제거)
원래 `watchlist_price_history.csv` 기반으로 만들었던 기능(하락률=고점대비 최대낙폭,
횡보=구간 변동폭). 만들 때는 DB 도입을 검토했다가 규모가 작다는 이유로 기각하고 CSV+git으로
갔었는데, 2026-08-19에 실제로 확인해보니 **그 CSV가 한 번도 쌓인 적이 없었음**(파일 자체가
git에 커밋된 적도 없음 — "Fishing 새로고침"을 사람이 눌러야만 쌓이는 구조가 실제로는 안
지켜지고 있었음). 매일 자동으로 쌓이는 §6-9의 Supabase 필터 빌더로 완전히 대체하고 이
CSV 파이프라인(`load_watchlist_history`/`snapshot_watchlist_history`/`find_pattern_matches`,
`WATCHLIST_HISTORY_FILE`)은 코드까지 삭제함. **재도입하지 말 것** — §6-9가 상위 대체재.

---

## §6-9. Supabase 파이프라인 — cron 지연 경위, 필터 빌더 제거 경위

### cron 시각이 07:00 UTC(정각)이 아니라 07:13인 이유
원래 정각으로 만들었는데, 2026-08-19에 예정(16:00 KST)보다 **2시간 3분 지연**(실제 18:03
KST 실행)되는 걸 겪음 — GitHub 공식 문서에 "정각에 몰린 예약 워크플로우는 특히 지연이
심하다"고 명시돼있는 문제라서, 애매한 분(13분)으로 옮겨서 회피함(2026-08-20).

### (제거됨) 필터 빌더 UI (2026-08-19 신설 → 2026-08-24 제거)
`ui_portfolio_tab.py`의 Fishing expander 안, ±3% 스크리너 밑에 조건 행을 자유롭게 추가/삭제
하며 AND로 조합하는 화면이 있었음(조건 종류: 등락률[기간+방향+값], 가격[이하/이상+값]).
계산 방식: 등락률 조건은 그 기간 **시작 종가 대비 끝 종가의 변화율**로 판정(고점 기준
최대낙폭이 아니라 구간 양끝 단순 비교) — 상승="+값% 이상", 하락="-값% 이하",
횡보="|변화율|≤값%". 기본값: 40영업일/하락/20% + 4영업일/횡보/5% ("장기 하락 + 최근 며칠
횡보" 조합).

**2026-08-24에 사용자 요청으로 UI와 로직(`run_filter_builder()`, `FILTER_CONDITION_TYPES`,
`FILTER_DIRECTIONS`)을 통째로 제거함** — 이유: DB에 데이터가 며칠~몇 주가 아니라 최소 몇
개월은 쌓여야 "등락률" 조건이 의미 있는 결과를 내는데, 그전까지는 그냥 죽어있는 화면이었고
조건 종류/계산 방식도 나중에 다시 구상해서 짜기로 함.

**재도입 시**: git log에서 이 날짜 전후 커밋을 찾아 코드를 참고할 것 —
`load_watchlist_history_db()`는 §6-6 "Fishing" 최초가 조회가 여전히 쓰고 있어서 안 지우고
남겨둠(필터 빌더 재구현 시 이것부터 재사용 가능). `portfolio_core.py`가 Streamlit에 안
물리게 유지하는 원칙(supabase_url/key를 인자로 받고 st.secrets는 UI 쪽에서만 읽음)은 남은
함수들도 그대로 따르고 있음.

**DB에 데이터가 더 쌓이기 전까지의 한계 (재도입 판단 기준)**: "등락률" 조건(기간 2일 이상
필요)은 데이터가 짧으면 의미 있는 결과가 안 나옴 — "가격" 조건은 하루치만 있어도 바로 동작.
"N영업일"은 **달력 날짜가 아니라 DB에 실제로 쌓인 행 개수** 기준인데, 매 평일 자동으로
쌓이므로(사람이 안 눌러도 됨) 실제 영업일과 거의 일치함(주말/공휴일에는 cron이 평일에만 돎,
`cron: "0 7 * * 1-5"`; 다만 평일 공휴일에는 그날도 그냥 실행되어 전일과 같은 종가가 한 번 더
찍힘 — 특별히 걸러내는 로직 없음, 사소한 한계로 남겨둠).

---

## §6-10. WATERING 칩 — 긴 종목명과 겹치던 문제, 픽셀 디버깅 경위

**처음엔(2026-08-21) 카드 밑에 항상 보이는 전체너비 버튼**("▼ 매수/매도 내역 · 물타기
그래프")이었는데, 종목이 51개까지 늘면서 화면이 너무 길어진다는 이유로 **2026-08-24에 카드
우상단 "WATERING" 칩으로 대체**함. `.stock-card` HTML 자체(카드 전체를 클릭 가능하게 만드는
것)는 여전히 안 건드림 — Streamlit이 순수 HTML(`st.markdown`)에 클릭 콜백을 못 걸기 때문에
카드 전체 클릭은 여전히 CSS 오버레이 편법이 필요해서 보류. 대신 카드 우상단(섹터태그 왼쪽)에
실제 버튼을 CSS로 절대위치시키는 절충안을 택함 — Streamlit이 `key=`를 주면 `st-key-<key>`
CSS 클래스를 자동으로 붙여주는 걸 이용(카드 전체를 덮는 오버레이보다 훨씬 안정적인 이유: 이
위치는 카드 내용과 무관하게 항상 카드 맨 위 고정 높이 줄이라, 종목명 길이/그리드 내용이
달라져도 안 깨짐).

**긴 종목명과의 겹침 (발생했다가 수정됨)**: 처음엔 종목명 옆에 비중까지 같이 두고
`right:108px` 고정값으로 WATERING을 얹었는데, 실제 화면(브라우저 확장 연결 안 돼서 한동안
육안 확인을 못 하다가, Playwright+로컬 Chrome으로 직접 렌더링해서 발견함,
[[project_playwright_visual_check]])에서 "신세계인터내셔날"처럼 긴 이름이 WATERING과 겹치는
게 확인됨 — absolute 포지션이라 종목명이 실제로 얼마나 긴지와 무관하게 고정 위치에 얹히기
때문. 근본 해결: (1) 비중을 이 줄에서 빼서 `stock-grid`의 "수량" 칸 밑으로 이동, (2)
종목명 폰트를 15px→13px로 축소 + 예약폭(`max-width:calc(100% - 180px)`) 재조정 +
`white-space:nowrap`. 실제 보유종목 중 가장 긴 이름(신세계인터내셔날, 8자)으로 렌더링 검증
완료(2026-08-24).

WATERING 칩 색상/굵기도 한 번 바뀜: 처음엔 회색 바탕에 파란 글씨 + 굵게였다가, 사용자 요청으로
검정(`T['text']`) + 일반굵기(400)로 변경 — 비중/현재가 등 다른 보조 텍스트와 톤을 맞추기 위함.

---

## §6-11. 자동화 테스트 — 도입 동기, 첫 CI에서 잡은 실전 버그

### 왜 그 시점에 도입했나
2026-08-18에 "자동화 테스트 필요해지면 먼저 알려달라"는 요청을 받았었고, §6-10 작업(사이클
필터링/계단식 평단가)을 검증하면서 실제 데이터로 즉석 스크립트를 짜서 수동으로 확인하는 데
시간을 꽤 쓴 뒤 — 이제 그 검증을 코드로 고정해둘 만한 시점이라고 판단해서 사용자에게 제안했고
동의를 받아 도입함. 지금까지의 검증 방식(세션마다 스크래치 스크립트 짜서 실제 데이터로
확인하고 버림)은 매번 다시 짜야 하고, 세션이 끝나면 그 검증 자체가 사라진다는 한계가 있었음.

### 도입 첫 CI 실행에서 바로 실전 버그를 잡음 (2026-08-24)
로컬(pandas 2.3.3)에서는 17개 다 통과했는데, GitHub Actions(신선한 `pip install`이라 그
시점 최신 버전이 깔림)에서 돌리니 **막 나온 pandas 3.0.5**가 설치되면서
`rebuild_portfolio_from_transactions`가 `TypeError: Invalid value '800.0' for dtype 'str'`로
실패함 — pandas 3.0부터 전부 빈 문자열인 컬럼("실현손익", 매수 행은 ""·매도 행은 숫자)을
Arrow 기반 문자열 dtype으로 추론해버려서, 나중에 숫자를 대입하면 막힘.
`tx["실현손익"] = tx["실현손익"].astype(object)`로 대입 직전에 object dtype으로 못박아서
고침(`portfolio_core.py`).

**중요**: `requirements.txt`가 원래 `pandas>=2.0`으로 상한이 없었음 — 즉 이 버그는 CI뿐
아니라 **Streamlit Cloud가 의존성을 새로 설치하는 어느 시점에든 실제 운영 앱에서도 터질 수
있는 문제**였음(§1-5 원칙과 별개로, "의존성 버전"도 재배포 때마다 바뀔 수 있는 불확실성이라는
걸 보여준 사례). pandas 3.0은 문자열 dtype 기본 동작이 크게 바뀐 메이저 릴리스라 이 한 군데
말고 다른 곳에도 비슷한 문제가 숨어있을 수 있다고 보고, `requirements.txt`의 pandas를
`>=2.0,<3.0`으로 상한을 걸어 예방함 — pandas 3.0 마이그레이션은 나중에 필요해지면 제대로
검증하고 별도로 진행할 것, 지금은 그냥 막아둔 상태.

테스트 도입 첫날 바로 이런 걸 잡아낸 게, 자동화 테스트를 하자고 한 판단이 맞았다는 근거.

AppTest 관련 사소한 버그도 같이 발견: `AppTest.from_file("app.py")`의 상대경로는 **호출한
테스트 파일 기준**으로 풀림(cwd 기준 아님) — `tests/test_app_smoke.py`에서 그냥 `"app.py"`라고
쓰면 `tests/app.py`를 찾다가 `FileNotFoundError`. 로컬에서 `python -c "..."`로 즉석 검증할
때는 `<string>`이 호출 파일 취급되며 우연히 cwd 기준으로 풀려서 안 걸렸던 것 — 진짜 pytest
파일 안에서 처음 걸림. `Path(__file__).resolve().parent.parent / "app.py"`로 절대경로를
명시해서 고침.

---

## §6-12. 거래량/외국인 수급 — 위닉스 이상치 사례, 검증 기록

### 위닉스 이상치 사례 (평균이 왜곡됐던 실제 사례)
위닉스가 평균 대비 -96%로 나온 적이 있었는데, 실제로는 그냥 3주 전(7/24~8/4) 급증했던 게
진정되며 완만히 줄어드는 추세였을 뿐이었음 — 어제 대비 -24%가 훨씬 실상에 가까웠음. "평균"이
쌓인 기간(약 20일) 전체의 단순평균이다 보니, 그 안에 낀 이상치 구간 하나가 평균을 크게
왜곡시킨 사례. 사용자가 "일단 데이터 더 쌓이면 나아질 문제"라고 판단해서 지금은 안 고치기로 함
(`CLAUDE.md` §6-12의 "알려진 한계" 참고).

### 검증 기록
`portfolio_core.py`의 새 함수들(`fetch_quotes`의 volume 파싱, `fetch_investor_flow`,
`fetch_market_flow`, `load_investor_flow_db`, `load_market_flow_db`, `compute_volume_flags`,
`compute_foreign_flags`, `compute_market_flow_baseline`) 전부 실제 라이브 데이터(153종목
백필분)로 검증함(2026-08-24). UI는 Playwright(로컬 Chrome,
[[project_playwright_visual_check]])로 실제 렌더링까지 확인 완료.

---

## §6-14. (만들었다가 제거됨) Shadow Asset (2026-08-25 신설 → 같은 날 제거)

**개념**: 사용자가 만든 지표로, "지금 미실현손실 상태인 종목들을 손절하지 않고 물타기로
결국 원가까지 회복시킨다"는 전제 하의 가상 총자산. 공식: **Shadow Asset = 총자산 +
미실현손실**(예: 총자산 900만원, 미실현손실 300만원이면 1200만원). 처음 사용자가
"총자산 + 실현손익누적"이라고 말했는데 본인이 든 예시로 역산하니 안 맞아서(900+200=1100
≠ 의도한 1200) "미실현손실"의 오타였음을 확인하고 진행함.

**구현은 새 계산이 전혀 필요 없었음** — `asset_history.csv`의 "조정자산" 컬럼이 처음부터
`snapshot_history(total_assets, total_assets + unrealized_loss)`로 저장되고 있던 값이
정확히 이 공식과 같아서(원래는 실현손익 그래프에서 "미실현손실 = 조정자산 − 총자산"을
뽑아내는 용도로만 쓰였음), 그 컬럼을 그대로 갖다 썼음.

**제거 이유**: 포트폴리오 탭 "섹터 비중 보기" 위에 카드+그래프(파란선=총자산,
빨간선=Shadow Asset, 원금 수평선)로 만들었는데, **거래 기록 탭의 기존 "실현손익
그래프"(미실현손실 라인이 이미 있음)와 내용이 겹친다고 판단**해서 만든 당일 제거함.
코드는 git log에서 커밋(`6bd1681` 추가 → `0b145f4` 제거)을 찾으면 됨.

**재도입 시 참고**: 데이터 소스(조정자산 컬럼)는 그대로 살아있으니 계산 로직은 재사용
가능 — 다시 만들 거면 "거래 기록 탭 그래프와 안 겹치게" 다른 표현 방식(예: 카드형 숫자만,
그래프 없이)을 고려할 것.
